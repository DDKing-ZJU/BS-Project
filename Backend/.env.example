# JWT配置
JWT_SECRET_KEY=your-secret-key-here
JWT_ALGORITHM=HS256

# 数据库配置
DB_DRIVER=mysql+pymysql
DB_USER=root
DB_PASSWORD=your-password
DB_HOST=localhost
DB_PORT=3306
DB_NAME=bs_project

# 完整的数据库URL（如果需要自定义，可以直接设置这个变量）
# DATABASE_URL=${DB_DRIVER}://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# 其他配置
FLASK_ENV=development
PORT=5000
